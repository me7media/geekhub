{% extends 'base.html.twig' %}

{% block title %}Edit Comment{% endblock %}

{% block body %}
    <h1>Edit Comment</h1>

    {{ include('@Catalog/comment/_form.html.twig', {'button_label': 'Update'}) }}
{% if is_granted('edit', comment) %}
    {{ include('@Catalog/comment/_delete_form.html.twig') }}
{% endif %}
    <a href="{{ path('comment_index') }}">back to list</a>
{% endblock %}
